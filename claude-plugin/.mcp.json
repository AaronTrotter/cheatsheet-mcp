{
  "mcpServers": {
    "cheatsheet": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.js"],
      "env": {
        "CHEATSHEET_ENV_FILE": "${CLAUDE_PLUGIN_DATA}/.env.local"
      }
    }
  }
}
